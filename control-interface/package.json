{
    "name": "pibard-control",
    "version": "1.0.0",
    "description": "PiBard Control Interface",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "nodemon server.js",
        "build": "mkdir -p dist && cp -r public/* dist/"
    },
    "dependencies": {
        "express": "^4.18.2",
        "socket.io": "^4.7.2",
        "node-snapcast-client": "^1.0.5",
        "node-pulseaudio-control": "^1.0.1"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}